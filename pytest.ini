[pytest]

testpaths = tests

python_files = test_*.py

python_classes = Test*

python_functions = test_*

addopts = -v -s --tb=short

markers =
    smoke: Critical tests to verify main application functionality
    sanity: Quick tests after small changes
    regression: Full test suite before release